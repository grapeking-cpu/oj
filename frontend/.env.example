# 前端开发环境配置

# Vite 开发服务器代理配置 (vite.config.ts)
# 不需要在此处配置，代理在 vite.config.ts 中设置
# 开发时前端访问 http://localhost:3000，会代理到 localhost:18080 (Docker 后端)

# Docker 部署时的端口配置
# API: http://localhost:18080 (宿主机访问)
# Frontend: http://localhost:80 (宿主机访问)
