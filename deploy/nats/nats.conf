# NATS basic ports
port: 4222
http_port: 8222

# JetStream enable + resource limits (recommended)
jetstream {
  store_dir: "/data/jetstream"
  max_mem: 256M
  max_file: 2G
}

# Limits (commented out due to config compatibility)
# limits {
#   max_payload: 1Mb
# }
